#[cite_start]This project demonstrates the core fundamentals of **Node.js**, including project initialization, package management with `npm`, and the creation of custom modules. [cite: 4, 5]

## 🚀 Key Objectives
* [cite_start]Understand the Node.js runtime environment and its non-blocking I/O model. [cite: 8, 12]
* [cite_start]Use `npm` to manage project dependencies. [cite: 16, 18]
* [cite_start]Implement module exports and imports within a local directory. [cite: 58, 60]

---

## 🛠️ Project Setup

### 1. Initialization
[cite_start]The project was initialized using `npm init`, which created the `package.json` metadata file. [cite: 19, 21, 28]
```bash
npm init -y
